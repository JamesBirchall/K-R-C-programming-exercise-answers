#include <stdio.h>
#include <ctype.h>
#include <string.h>

void ex3_1();

int binsearch(int, int[], int);
int newbinsearch(int, int[], int);
int atoi2(char []);
void shellsort(int[], int);
void reverseString(char[]);
